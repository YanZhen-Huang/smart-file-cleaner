# 过期文件定时清理 - 使用说明

本工具基于 [yangbin09/smart-file-cleaner](https://github.com/yangbin09/smart-file-cleaner) 改造，
新增了「按文件名日期判定过期」「删除前先进回收站」「无人值守定时清理」能力，
并提供了**桌面 GUI**（目录/格式选择、定时设置、报告查看、开机自启、托盘后台运行）。

## 快速开始

### 1. 安装依赖

```bash
pip install -r requirements.txt
pip install pystray pillow   # GUI 托盘所需（可选，缺失则自动降级为仅窗口）
```

### 2. 启动界面

```bash
# 默认启动桌面 GUI（推荐）
python main.py

# 后台托盘模式（无窗口，供开机自启调用）
python main.py --tray

# 无人值守自动模式（按配置直接执行，供外部定时任务调用）
python main.py --auto

# 预览模式（只扫描标记，不删除任何文件）
python main.py --preview
```

### 3. GUI 使用

| 区域 | 说明 |
|------|------|
| 目标目录 | 添加/移除要清理的目录（可多个） |
| 过期规则 | 过期天数、是否优先用文件名日期判定 |
| 清理文件格式 | 勾选文档/图片/临时文件大类，或填自定义扩展名（逗号分隔，如 `.zip, .rar`） |
| 删除安全 | 移入回收站、删除前先备份（建议都勾选） |
| 定时与自启 | 每天定时自动清理（填时间 HH:MM）；勾选开机自启则写注册表 Run 键，开机后以 `--tray` 后台运行 |
| 立即预览 / 立即清理 | 按当前界面设置执行 |
| 清理报告 | 左侧列出 reports 目录下的历史报告，选中后点「查看选中」在右侧输出区显示 |

> 勾选/修改任何设置后点「保存设置」即写入 `src/config/operation-config.json`；
> 定时清理与开机自启即时生效，无需重启程序。

### 4. 手工编辑配置（可选）

编辑 `src/config/operation-config.json`：

```json
{
  "processing_config": {
    "processor_type": "smart_document",
    "cleaning_strategy": "age",
    "target_directories": ["C:/Users/YOURNAME/Downloads"],
    "max_age_days": 30,
    "use_filename_date": true,
    "target_extensions": [],
    "safety_settings": {
      "create_backup": true,
      "backup_dir": "backups",
      "use_recycle_bin": true,
      "require_confirmation": false
    }
  },
  "report_settings": {
    "generate_report": true,
    "output_dir": "reports"
  },
  "auto_clean": {
    "enabled": false,
    "time": "02:00",
    "start_with_windows": false
  }
}
```

配置项说明：

| 配置项 | 说明 |
|--------|------|
| `target_directories` | 要清理的目录列表，可配多个 |
| `max_age_days` | 过期天数，超过该天数即视为过期 |
| `use_filename_date` | 是否优先用**文件名中的日期**判定（如 `2024-01-15 报告.docx`、`20240115_report.pdf`）；无日期或解析失败时退回用最后修改时间 |
| `target_extensions` | 限定的扩展名列表，留空 `[]` 表示文档+图片+临时文件全部参与 |
| `create_backup` | 删除前是否先复制一份到 `backup_dir` |
| `use_recycle_bin` | 是否移入 Windows 回收站（可恢复，强烈建议保留 true） |
| `require_confirmation` | 交互模式下删除前是否询问 |
| `auto_clean.enabled` | 是否启用内置定时自动清理 |
| `auto_clean.time` | 每天定时执行时间（HH:MM） |
| `auto_clean.start_with_windows` | 是否开机自启（后台托盘运行） |

### 5. 注册 Windows 定时任务（备选）

如果不想用内置定时调度，也可右键 **「注册定时清理任务.bat」** 以管理员身份运行，
注册一个**每日 02:00** 的自动清理任务。

- 立即手动触发一次：`schtasks /Run /TN "SmartFileCleaner"`
- 卸载：`schtasks /Delete /F /TN "SmartFileCleaner"`

## 过期判定逻辑

每个文件按以下顺序判定：

1. 扩展名不在允许范围内 → 跳过（代码/配置/程序文件永远不删）
2. 开启 `use_filename_date` 且文件名含日期（支持 `2024-01-15`、`2024_1_15`、`2024年1月15日`、`20240115`）→ 按该日期计算年龄
3. 否则按文件最后修改时间计算年龄
4. 年龄 > `max_age_days` → 过期，进入删除流程（先备份 → 移入回收站）

## 改造清单（相对上游）

- `src/core/cleaning-strategies.py`：`AgeBasedCleaningStrategy` 支持文件名日期解析；新增 `create_age_cleaning_context()`
- `src/core/processor-factory.py`：`BaseProcessor` 补 `scan_directory()`；`_delete_file()` 支持备份 + 移入回收站（PowerShell Shell API）；修复 `get_file_info` 缺失 bug
- `src/core/file-processor-controller.py`：补 `run_interactive_mode()` 入口；新增 `run_auto_mode()` 无人值守模式、`run_preview_mode()` 预览模式；`_process_directory()` 由占位改为真实执行；自动清理结果写 JSON 报告到 `reports/`
- `main.py`：新增 `--auto`/`--preview`/`--tray` 参数；默认启动 GUI；重配 UTF-8 输出
- 新增 `src/gui/`：`app.py`（tkinter 主窗口：目录/格式/规则/定时/自启/报告）、`scheduler.py`（内置每日定时调度线程）、`tray.py`（pystray 托盘图标）
- 新增 `src/config/operation-config.json`（已被 .gitignore 排除，本地专属配置）
- 新增 `注册定时清理任务.bat`