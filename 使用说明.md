# 智能文档和图片清理器 使用说明

## 概述

智能文档和图片清理器是一个专门用于清理项目目录中文档和图片文件的工具，同时智能保护代码文件、配置文件和程序文件。

## 功能特点

### ✅ 会被删除的文件类型

**文档文件:**
- `.txt`, `.doc`, `.docx`, `.pdf`, `.rtf`, `.odt`, `.pages`
- `.xls`, `.xlsx`, `.ppt`, `.pptx`, `.odp`, `.ods`
- `.md`, `.markdown`, `.rst`, `.tex`, `.latex`

**图片文件:**
- `.jpg`, `.jpeg`, `.png`, `.gif`, `.bmp`, `.tiff`, `.tif`
- `.webp`, `.svg`, `.ico`, `.psd`, `.ai`, `.eps`, `.raw`
- `.cr2`, `.nef`, `.arw`, `.dng`, `.heic`, `.heif`

### 🛡️ 会被保护的文件类型

**代码文件:**
- `.py`, `.js`, `.java`, `.c`, `.cpp`, `.h`, `.hpp`
- `.cs`, `.php`, `.rb`, `.go`, `.rs`, `.swift`, `.kt`
- `.ts`, `.jsx`, `.tsx`, `.vue`, `.html`, `.css`, `.scss`
- `.sass`, `.less`, `.sql`, `.sh`, `.bat`, `.ps1`, `.vbs`

**配置文件:**
- `.config`, `.conf`, `.cfg`, `.ini`, `.env`, `.properties`
- `.yaml`, `.yml`, `.toml`, `.xml`, `.json`, `.lock`
- `.gitignore`, `.gitattributes`, `.editorconfig`

**程序文件:**
- `.exe`, `.dll`, `.so`, `.dylib`, `.app`, `.deb`, `.rpm`
- `.msi`, `.pkg`, `.dmg`, `.iso`, `.jar`, `.war`, `.ear`

**重要项目文件:**
- `README`, `LICENSE`, `CHANGELOG`, `Makefile`, `Dockerfile`
- `requirements.txt`, `package.json`, `package-lock.json`
- `yarn.lock`, `composer.json`, `Gemfile`, `Pipfile`等

## 使用方法

### 方法一：图形化交互界面（推荐）

1. **Windows用户:**
   - 双击 `启动清理器.bat` 文件
   - 或者双击 `启动清理器.ps1` 文件

2. **其他系统用户:**
   ```bash
   python start_cleaner.py
   ```

3. **按照提示操作:**
   - 选择是否查看文件类型说明
   - 输入要清理的目录路径（默认当前目录）
   - 选择清理模式：
     - `1. 仅预览` - 查看将要删除的文件，不实际删除
     - `2. 预览并清理` - 先预览，确认后执行删除
     - `3. 预览并清理(带备份)` - 先预览，确认后执行删除并创建备份

### 方法二：命令行模式

```bash
# 基本用法 - 仅预览
python smart_document_cleaner.py --dry-run

# 指定目录并预览
python smart_document_cleaner.py /path/to/directory --dry-run

# 实际删除（会先预览再确认）
python smart_document_cleaner.py /path/to/directory

# 删除并创建备份
python smart_document_cleaner.py /path/to/directory --backup

# 跳过确认直接删除
python smart_document_cleaner.py /path/to/directory --force
```

### 命令行参数说明

- `directory`: 要清理的目录路径（可选，默认当前目录）
- `--dry-run`: 模拟模式，只预览不实际删除
- `--backup`: 删除前创建备份
- `--force`: 跳过确认直接执行

## 安全特性

1. **智能识别**: 自动识别文件类型，避免误删重要文件
2. **预览模式**: 默认先预览要删除的文件，用户确认后再执行
3. **备份功能**: 可选择在删除前创建备份
4. **详细报告**: 生成详细的清理报告，记录所有操作
5. **错误处理**: 完善的错误处理机制，避免程序崩溃

## 输出文件

### 清理报告
程序会自动生成清理报告文件：`cleanup_report_YYYYMMDD_HHMMSS.json`

报告内容包括：
- 删除文件统计
- 删除文件列表
- 错误文件列表
- 释放的磁盘空间

### 备份文件
如果选择创建备份，会生成备份目录：`backup_YYYYMMDD_HHMMSS/`

## 注意事项

1. **首次使用建议先运行预览模式**，确认要删除的文件无误
2. **重要文件请提前备份**，虽然程序有保护机制，但建议谨慎操作
3. **大目录清理可能需要较长时间**，请耐心等待
4. **网络驱动器或权限受限的目录**可能无法正常清理

## 系统要求

- Python 3.6 或更高版本
- Windows/Linux/macOS 操作系统
- 足够的磁盘空间（如果创建备份）

## 故障排除

### 常见问题

1. **"未找到Python"错误**
   - 请安装Python 3.6或更高版本
   - 确保Python已添加到系统PATH环境变量

2. **权限不足错误**
   - 以管理员身份运行程序
   - 检查目标目录的访问权限

3. **文件被占用无法删除**
   - 关闭正在使用这些文件的程序
   - 重启计算机后再次尝试

4. **程序运行缓慢**
   - 大目录扫描需要时间，请耐心等待
   - 可以先在小目录上测试

### 获取帮助

如果遇到问题，请检查：
1. 清理报告中的错误信息
2. 控制台输出的详细错误信息
3. 确认Python环境是否正确安装

## 版本信息

- 版本: 1.0
- 更新日期: 2024年
- 兼容性: Python 3.6+

---

**免责声明**: 请在使用前仔细阅读本说明，确保了解程序的工作原理。虽然程序设计了多重保护机制，但建议在重要数据上使用前先进行测试。